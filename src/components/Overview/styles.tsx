import { makeStyles } from '@mui/styles';

export default makeStyles(() => ({
  overviewMain: {
    padding: '0px 20px',
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
  },
  gitCalendar: {
    display: 'flex',
    justifyContent: 'center',
    padding: '40px 0px',
  },
}));
